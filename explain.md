# Week 5: JavaScript Fundamentals - Lab Summary

## แสดงผลลัพธ์ และอธิบายการทำงานของโค๊ดว่าแสดงผลลัพธ์มาได้อย่างไร

### ไฟล์ 01-variables.js ✅

### 6. Challenge: Create a Person Object

โค้ดนี้เป็นการสร้าง Object ชื่อ `student` เพื่อเก็บข้อมูลของนักเรียนและเมธอดที่เกี่ยวข้อง

#### การทำงานของโค้ด

- สร้าง Object `student` ที่มีข้อมูลส่วนตัว เช่น ชื่อ อายุ GPA และรายวิชาที่เรียน
- ใช้เมธอด `getFullName()` เพื่อรวมชื่อและนามสกุล โดยอ้างอิงข้อมูลภายใน Object ด้วย `this`
- เมธอด `getInfo()` เรียกใช้ `getFullName()` และแสดงข้อมูลเพิ่มเติม เช่น อายุและ GPA
- ใช้ `join(", ")` เพื่อแสดงรายวิชาใน Array ให้อยู่ในรูปแบบข้อความ

#### ผลลัพธ์ที่ได้

```text
=== Challenge: Person Object ===
Student object:
{
  firstName: 'Alice',
  lastName: 'Smith',
  age: 20,
  gpa: 3.8,
  courses: [ 'HTML', 'CSS', 'JavaScript' ],
  isActive: true,
  getFullName: [Function: getFullName],
  getInfo: [Function: getInfo]
}
Full name: Alice Smith
Info: Alice Smith, Age: 20, GPA: 3.8
Courses: HTML, CSS, JavaScript
```

### ไฟล์ 02-functions.js ✅

### 8. Returning Objects

โค้ดนี้เป็นการสร้างฟังก์ชัน `createUser` เพื่อสร้างและส่งกลับ Object ของผู้ใช้

#### การทำงานของโค้ด

- ฟังก์ชัน `createUser(firstName, lastName, age)` รับชื่อ นามสกุล และอายุ
- ใช้ shorthand property ในการสร้าง Object
- สร้าง `email` จากชื่อและนามสกุลโดยแปลงเป็นตัวพิมพ์เล็ก
- มีเมธอด `getFullName()` สำหรับแสดงชื่อเต็ม
- มีเมธอด `getAge()` สำหรับคืนค่าอายุ

#### ผลลัพธ์ที่ได้

```text
Returning Objects:
{
  firstName: 'John',
  lastName: 'Doe',
  age: 30,
  email: 'john.doe@example.com',
  getFullName: [Function: getFullName],
  getAge: [Function: getAge]
}
Email: john.doe@example.com
Full name: John Doe
```

### 9. Function as Parameter (Callback)

โค้ดนี้แสดงการใช้ **Callback Function** โดยการส่งฟังก์ชันเข้าไปเป็นพารามิเตอร์ของฟังก์ชันอื่น

#### การทำงานของโค้ด

- ฟังก์ชัน `processArray(arr, callback)` รับ Array และฟังก์ชัน callback
- ใช้ `for...of` วนลูปแต่ละค่าใน Array
- เรียกใช้ `callback(item)` เพื่อประมวลผลข้อมูล
- เก็บผลลัพธ์ใหม่ลงใน Array `result` และส่งกลับด้วย `return`
- ส่ง Arrow Function เข้าไปเป็น callback เพื่อคำนวณค่าแตกต่างกัน

#### ผลลัพธ์ที่ได้

```text
Callback Function:
Original: [ 1, 2, 3, 4, 5 ]
Doubled: [ 2, 4, 6, 8, 10 ]
Squared: [ 1, 4, 9, 16, 25 ]
```

### ไฟล์ 03-control-flow.js ✅

### 5. Short-Circuit Evaluation

โค้ดนี้แสดงการทำงานของ **Short-Circuit Evaluation** ซึ่งเป็นการหยุดประเมินเงื่อนไขทันทีเมื่อรู้ผลลัพธ์แล้ว โดยใช้ตัวดำเนินการ `||` และ `&&`

#### การทำงานของโค้ด

- ใช้ `||` (OR) เพื่อเลือกค่าที่เป็นจริงตัวแรก
- ใช้ `?.` (Optional Chaining) เพื่อเข้าถึง property ของ object อย่างปลอดภัย
- ใช้ `&&` (AND) เพื่อตรวจสอบว่ามี object ก่อนเข้าถึง property

#### อธิบายทีละส่วน

- `admin?.name`  
  เนื่องจาก `admin` เป็น `null` จึงไม่เกิด error และคืนค่า `undefined`
- `undefined || user.name`  
  `user.name` มีค่าเป็น `"John"` จึงถูกเลือกมาใช้
- ผลลัพธ์สุดท้ายของ `userName` คือ `"John"`

- `user && user.profile`  
  `user` มีค่าเป็นจริง แต่ `user.profile` ไม่มีอยู่ จึงได้ค่า `undefined`

#### ผลลัพธ์ที่ได้

```text
Short-Circuit Evaluation:
User name: John
User profile: undefined
```

### 7. Form Validation

โค้ดนี้ใช้สำหรับตรวจสอบความถูกต้องของข้อมูลที่ผู้ใช้กรอกในฟอร์ม (Form Validation)

#### การทำงานของโค้ด

- ฟังก์ชัน `validateRegistration(formData)` รับข้อมูลฟอร์มในรูปแบบ Object
- สร้าง Array `errors` เพื่อเก็บข้อความ error
- ตรวจสอบข้อมูลแต่ละช่องด้วย `if-else`
- ถ้ามี error อย่างน้อย 1 รายการ จะถือว่าข้อมูลไม่ถูกต้อง
- ส่งผลลัพธ์กลับเป็น Object ที่มีสถานะและรายละเอียด error

#### เงื่อนไขที่ใช้ตรวจสอบ

- `name` ต้องไม่ว่างและมีอย่างน้อย 3 ตัวอักษร
- `email` ต้องมีเครื่องหมาย `@`
- `age` ต้องมีค่าอย่างน้อย 18 ปี
- `password` ต้องมีอย่างน้อย 6 ตัวอักษร
- ต้องยอมรับเงื่อนไข (`agreeToTerms` เป็น true)

#### ผลลัพธ์ที่ได้

```text
Form Validation:
Valid user: { isValid: true, errors: [] }
Invalid user: {
  isValid: false,
  errors: [
    'Name must be at least 3 characters',
    'Valid email is required',
    'Must be 18 or older',
    'Password must be at least 6 characters',
    'Must agree to terms'
  ]
}
```

### ไฟล์ 04-loops.js ✅

### 9. Chaining Methods

โค้ดนี้แสดงการใช้ **Method Chaining** โดยเรียกใช้หลาย method ของ Array ต่อเนื่องกันในบรรทัดเดียว

#### การทำงานของโค้ด

- `filter()` คัดเลือกเฉพาะเลขคู่
- `map()` แปลงข้อมูลเป็นข้อความแสดงค่ากำลังสอง
- `join()` รวม Array ให้เป็น String เดียว
- `reduce()` ใช้คำนวณค่าเฉลี่ยของตัวเลข

#### ผลลัพธ์ที่ได้

```text
Method chaining:
Even numbers squared: 2²=4, 4²=16, 6²=36, 8²=64, 10²=100
Average: 30
```

### 10. Challenge: Student Grades

โค้ดนี้เป็นการวิเคราะห์ข้อมูลนักเรียน โดยใช้ Array Methods หลายแบบร่วมกัน

#### การทำงานของโค้ด

1. **Get all names**
   - ใช้ `map()` ดึงชื่อของนักเรียนทั้งหมด
2. **Filter high scorers**
   - ใช้ `filter()` คัดนักเรียนที่ได้คะแนนตั้งแต่ 85 ขึ้นไป
   - แสดงผลชื่อและคะแนน
3. **Calculate class average**
   - ใช้ `reduce()` รวมคะแนนทั้งหมดแล้วหารด้วยจำนวนคน
4. **Find top scorer**
   - ใช้ `reduce()` เพื่อหานักเรียนที่ได้คะแนนสูงสุด
5. **Create summary**
   - สร้าง Object ใหม่โดยเพิ่มเกรด (`grade`)
     - ≥ 90 → "A"
     - ≥ 80 → "B"
     - ต่ำกว่า 80 → "C"
   - ใช้ `sort()` เรียงคะแนนจากมากไปน้อย
   - แสดงชื่อ คะแนน และเกรดแต่ละคน

#### ผลลัพธ์ที่ได้

```text
Challenge: Student Analysis
Students: [
  { name: 'Alice', score: 95 },
  { name: 'Bob', score: 75 },
  { name: 'Charlie', score: 85 },
  { name: 'Diana', score: 92 },
  { name: 'Eve', score: 88 }
]
Names: Alice, Bob, Charlie, Diana, Eve
High scorers: Alice (95), Charlie (85), Diana (92), Eve (88)
Class average: 87.00
Top scorer: Alice (95)
Summary (sorted):
 Alice: 95 (A)
 Diana: 92 (A)
 Eve: 88 (B)
 Charlie: 85 (B)
 Bob: 75 (C)

✅ Activity 4 completed!
```

### ไฟล์ 05-integration.js ✅

### Activity 5: Integration - Quiz Application

โค้ดนี้เป็นการสร้าง **Quiz Application แบบจำลอง** เพื่อประเมินคะแนนผู้ใช้และให้ผลลัพธ์พร้อมเกรด

#### การทำงานของโค้ด

1. **สร้าง Quiz Data**

   - Array `quizzes` เก็บคำถาม (`question`), ตัวเลือก (`options`) และคำตอบที่ถูกต้อง (`correctAnswer`)

2. **จำลองการทำ Quiz**

   - ใช้ `forEach()` เพื่อวนลูปแต่ละคำถาม
   - ใช้ `Math.random()` เลือกคำตอบของผู้ใช้แบบสุ่ม
   - เปรียบเทียบคำตอบผู้ใช้กับคำตอบที่ถูกต้อง
   - เก็บผลลัพธ์ใน Array `results` (รวมหมายเลขคำถาม, คำถาม, คำตอบผู้ใช้, คำตอบที่ถูกต้อง, และสถานะถูก/ผิด)

3. **แสดงผลลัพธ์แต่ละคำถาม**

   - แสดงสถานะ ✅ CORRECT หรือ ❌ WRONG
   - ถ้าผิดจะแสดงคำตอบที่ถูกต้อง

4. **คำนวณคะแนนและเกรด**

   - นับจำนวนคำตอบถูก (`correctCount`)
   - คำนวณคะแนนเป็นเปอร์เซ็นต์
   - กำหนดเกรดตามคะแนน:
     - ≥ 90 → A
     - ≥ 80 → B
     - ≥ 70 → C
     - ≥ 60 → D
     - < 60 → F

5. **ให้ Feedback**

   - คะแนน 100 → "Perfect score! Excellent work!"
   - 80–99 → "Great job! Keep practicing."
   - 60–79 → "Good effort. Review the material and try again."
   - < 60 → "Keep practicing. You'll improve!"

6. **สถิติการทำ Quiz**
   - แสดงจำนวนคำถามทั้งหมด, ถูก, ผิด, และเปอร์เซ็นต์ความสำเร็จ
   - แยกผลลัพธ์ตามความถูกต้อง (`correct` / `incorrect`) โดยใช้ `reduce()`

#### ผลลัพธ์ที่ได้ (เนื่องจากคำตอบสุ่ม ผลลัพธ์อาจแตกต่างกัน)

```text
🎯🎯 === QUIZ APPLICATION === 🎯🎯

QUIZ RESULTS:
────────────────────────────────────────────────────────────
Q1: What is 5 + 3?
 Your answer: 8
 ✅ CORRECT

Q2: What is the capital of Thailand?
 Your answer: Pattaya
 Correct answer: Bangkok
 ❌ WRONG

Q3: What is the largest planet?
 Your answer: Neptune
 Correct answer: Jupiter
 ❌ WRONG

Q4: What is 2^8?
 Your answer: 256
 ✅ CORRECT

Q5: Which is NOT a JavaScript data type?
 Your answer: symbol
 Correct answer: class
 ❌ WRONG

────────────────────────────────────────────────────────────
FINAL SCORE: 2/5 (40.0%)
GRADE: F

FEEDBACK:
💪💪 Keep practicing. You'll improve!

📊📊 STATISTICS:
Total questions: 5
Correct: 2
Incorrect: 3
Success rate: 40.0%

Answer breakdown:
 ✅ Correct: 2
 ❌ Incorrect: 3

✅ All activities completed!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```
